# Terraform state — contains cluster credentials and sensitive outputs
*.tfstate
*.tfstate.*

# Provider binaries and plugins
.terraform/

# Crash logs
crash.log
crash.*.log

# Claude skill definition (IDE tooling, not project code)
claude.md

# Override files (local developer overrides, not for source control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
